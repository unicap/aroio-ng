config BR2_PACKAGE_JACK_MINIMIX
	   bool "jack_minimix"
	   depends on BR2_TOOLCHAIN_HAS_THREADS
	   help
	     Jack Mixer using OSC

comment "liblo needs a toolchain w/ threads"
	depends on !BR2_TOOLCHAIN_HAS_THREADS
