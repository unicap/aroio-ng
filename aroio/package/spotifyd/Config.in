config BR2_PACKAGE_SPOTIFYD
	bool "spotifyd"
	depends on BR2_PACKAGE_HOST_RUSTC_TARGET_ARCH_SUPPORTS
	select BR2_PACKAGE_HOST_CARGO
	help
		Open source spotify client running as a daemon

		https://github.com/Spotifyd/spotifyd
